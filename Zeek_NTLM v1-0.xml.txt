<form version="1.1" theme="dark">
  <label>Zeek NTLM (V1.0)</label>
  <description>V1 Additions: First generation of dashboard</description>
  <fieldset submitButton="true" autoRun="true">
    <input type="time" token="Time">
      <label>Time Selection</label>
      <default>
        <earliest>-24h@h</earliest>
        <latest>now</latest>
      </default>
    </input>
  </fieldset>
  <row>
    <panel>
      <single>
        <title>Total NTLM Logs</title>
        <search>
          <query>index=<index> sourcetype="zeek:ntlm:json" NOT 
    [| inputlookup Known_Scanners.csv
    | rename scannerip AS id.orig_h
    | table id.orig_h
    | format]
| stats count</query>
          <earliest>$Time.earliest$</earliest>
          <latest>$Time.latest$</latest>
          <refresh>5m</refresh>
          <refreshType>delay</refreshType>
        </search>
        <option name="drilldown">none</option>
        <option name="refresh.display">progressbar</option>
      </single>
    </panel>
  </row>
  <row>
    <panel>
      <table>
        <title>NTLM Admin &amp; Domain Admin Authentication</title>
        <search>
          <query>index=<index> sourcetype="zeek:ntlm:json" NOT 
    [| inputlookup Known_Scanners.csv
    | rename scannerip AS id.orig_h
    | table id.orig_h
    | format]
| stats count by domainname,server_tree_name,server_dns_computer_name,src,dest,username
| eval domainname=upper(domainname)
| where !like (domainname, "$") 
| where !like (username, "%$")
| where like (username, "%.da") OR like (username, "%.adm") or like (username, "Administrator")
| sort by -count
| rename domainname AS "Domain Name", server_tree_name AS "Domain Address", server_dns_computer_name AS "FQDN", src AS "Source IP", dest AS "Destination IP", username AS User, count AS Count</query>
          <earliest>$Time.earliest$</earliest>
          <latest>$Time.latest$</latest>
          <refresh>5m</refresh>
          <refreshType>delay</refreshType>
        </search>
        <option name="drilldown">none</option>
        <option name="refresh.display">progressbar</option>
      </table>
    </panel>
  </row>
  <row>
    <panel>
      <table>
        <title>NTLM Service Account Authentication</title>
        <search>
          <query>index=<index> sourcetype="zeek:ntlm:json" NOT 
    [| inputlookup Known_Scanners.csv
    | rename scannerip AS id.orig_h
    | table id.orig_h
    | format]
| stats count by domainname,server_tree_name,server_dns_computer_name,src,dest,username
| eval domainname=upper(domainname)
| where !like (domainname, "$")
| where !like (username, "%$")
| where like (username, "%.svc") OR like (username, "%svc%")
| sort by -count
| rename domainname AS "Domain Name", server_tree_name AS "Domain Address", server_dns_computer_name AS "FQDN", src AS "Source IP", dest AS "Destination IP", username AS User, count AS Count</query>
          <earliest>$Time.earliest$</earliest>
          <latest>$Time.latest$</latest>
          <refresh>5m</refresh>
          <refreshType>delay</refreshType>
        </search>
        <option name="drilldown">none</option>
        <option name="refresh.display">progressbar</option>
      </table>
    </panel>
  </row>
</form>